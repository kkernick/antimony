name = "locale"
description = "Provide localization information"

[environment]
LANG = "$LANG"

[libraries]
directories = ["locale"]

[files.platform]
ro = ["/etc/localtime", "/usr/share/locale", "/usr/share/zoneinfo"]
