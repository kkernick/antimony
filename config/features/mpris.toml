name = "mpris"
description = "Give access to the MPRIS bus for Media Control"

[ipc]
talks = ["org.freedesktop.PowerManagement.*"]
owns = ["org.mpris.MediaPlayer2.{desktop}.*", "org.mpris.MediaPlayer2.{name}.*"]
