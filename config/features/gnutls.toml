name = "gnutls"
description = "TLS for secure network communications."
requires = ["gir", "locale"]

libraries = [
  "libgnutls*",
  "pkcs11"
]

[files]
platform.ro = [
  "/etc/gnutls",
  "/etc/pkcs11",
]
resources.ro = [
  "/usr/share/p11-kit/",
]
