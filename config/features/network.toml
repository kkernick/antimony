name = "network"
description = "Give applications network connectivity."
namespaces = ["Net"]

[files.platform]
ro = ["/etc/hosts", "/etc/pki", "/etc/resolv.conf", "/etc/nsswitch.conf", "/etc/gai.conf", "/etc/ca-certificates", "/etc/host.conf", "/etc/ssl"]

[files.resources]
ro = ["/usr/share/ssl", "/usr/share/pki", "/usr/share/ca-certificates"]
