name = "trace"
description = "Run the application underneath strace"
conditional = "command -v /usr/bin/strace"
sandbox_args = ["strace", "-ffyY", "-v", "-s", "256"]
binaries = ["strace"]

[hooks.parent]
type = "Program"
name = "tracer"
content = "$AT_HOME/utilities/antimony-tracer"
capture_error = true
env = true
