name = "shell"
description = "Provide /usr/bin/sh"
requires = ["passwd"]
binaries = ["/usr/bin/sh"]

[environment]
TERM = "$TERM"

[files.platform]
ro = ["/etc/profile"]

[files.platform]
ro = ["/usr/share/terminfo"]
