assert_eq () {
  if [[ $1 -ne $2 ]]; then
    exit 1
  fi
}

assert_ne () {
  if [[ $1 -eq $2 ]]; then
    exit 1
  fi
}

assert_str () {
  if [[ "$1" != "$2" ]]; then
      exit 1
  fi
}

assert () {
  if ! eval $@; then
      exit 1
  fi
}