[package]
name = "which"
version = "0.1.0"
edition = "2024"

[dependencies]
common = { path = "../common", features = ["cache"] }
dashmap = { version = "6.1.0", default-features = false }
rayon = { version = "1.11.0", default-features = false }

[lints]
workspace = true
