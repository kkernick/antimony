# Antimony Completions

This crate contains the logic needed to generate shell completions for Antimony. It is not intended for public use, though you may find some utility if you need to generate similar completions for your own project.

